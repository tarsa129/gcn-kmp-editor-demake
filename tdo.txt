better ways to handle cameras:
better type 3 rendering
- make relative to area

updates:
better area rendering
improve json files
when placing an object with a routepoint, assign to closest routepoint (new option)
error analyzer
move route to another tab for cameras
- add button for "create route" and "remove route"

new features:
handle boos automatically + handle that enemy point switching thing
scale on gizmo (better) + area scale handlers
add arrow keys for "next point" in route + linedraw increment
load + collide on gobj collision files
add option to select x to y points
create dialog for auto generation and auto cleanup
brres loading
