better ways to handle routes:
make camera types 1 and 2 a "route optional" camera + add in 4 and 5 as well

general fixes:
edit camera / area types boxes
keep track of variables for topdown and 3d modes individually

updates:
improve json files
came - camera viewing fixes + replay (all types of cameras)
render with scale
better opening camera copying + creation
when placing an object with a routepoint, assign to closest routepoint (new option)
error analyzer

new features:
handle boos automatically + handle that enemy point switching thing
add arrow keys for "next point" in route
load + collide on gobj collision files
add option to select x to y points
create dialog for auto generation and auto cleanup
brres loading